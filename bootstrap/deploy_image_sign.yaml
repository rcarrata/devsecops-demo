- name: 'Extend Original Demo to Include TaskRun and Image Signing'
  hosts: localhost
  connection: local
  tasks:

    # - name: 'Install NooBaa'
    #   include_role:
    #     name: "ocp4-install-noobaa"

    - name: 'Install Requirements for Image Signing'
      include_role:
        name: "ocp4-install-sign-patch"   
