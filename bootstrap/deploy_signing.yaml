---
- name: 'Extend Original Demo to Include TaskRun and Image Signing'
  hosts: localhost
  connection: local
  tasks:

    - name: 'Enable Tekton Chaining'
      include_role:
        name: "ocp4-install-signing"

    